<%= form_for(@player) do |f| %>
  <% if @player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% @player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fullName %><br>
    <%= f.text_field :fullName %>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <div class="field">
    <%= f.label :shoptechEmail %><br>
    <%= f.text_field :shoptechEmail %>
  </div>
  <div class="actions">
	  <%= f.submit %>
	  <%= f.submit 'Cancel', :class => "btn btn-danger", :data => { :dismiss => 'modal' }, :onclick => "form_submitted = true; return false;"%>
  </div>
<% end %>
